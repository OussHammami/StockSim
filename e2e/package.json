{
  "name": "stocksim-e2e",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "install:browsers": "playwright install --with-deps",
    "pretest": "wait-on http-get://localhost:8080/readyz http-get://localhost:8081/healthz http-get://localhost:5173",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/node": "^24.9.1",
    "@types/wait-on": "^5.3.4",
    "wait-on": "^7.0.1"
  }
}

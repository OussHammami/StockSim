@rendermode InteractiveServer
@using MudBlazor

<MudSwitch T="bool" @bind-Value="Value" Color="Color.Primary" Label="Dark mode" />

@code {
    [Parameter] public bool Value { get; set; }
    [Parameter] public EventCallback<bool> ValueChanged { get; set; }
}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-worker
  labels:
    app.kubernetes.io/name: trading-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: trading-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: trading-worker
    spec:
      containers:
        - name: trading-worker
          image: stocksim/trading-worker
          imagePullPolicy: IfNotPresent
          env:
            - name: Rabbit__Host
              value: rabbitmq
            - name: Rabbit__User
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-auth
                  key: username
            - name: Rabbit__Pass
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-auth
                  key: password
            - name: ConnectionStrings__TradingDb
              valueFrom:
                secretKeyRef:
                  name: stocksim-connectionstrings
                  key: TradingDb
